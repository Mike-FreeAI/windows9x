"use client";
/**
 * Functional component to display a welcome message
 * @param {Object} object - An object parameter
 * @param {String} object.id - A string parameter within the object used for identification
 * @returns {JSX.Element} A JSX element displaying the welcome message
 */
export function Welcome({ id: _id }: { id: string }) {
  return <div>Welcome to Windows 9X</div>;
}
